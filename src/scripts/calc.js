let newArray = []; //initialize a new array as the new container for our converted number strings


function reverseString(){ //3
 
}

function makeString(x) { //2
    x.toString(); 
    reverseString() //2.5
    return newArray;
  }
  
const yearEndTotal = [124, 1522, 126162];
const results = yearEndTotal.map((element) => makeString(element)); //1
  
console.log(results); // Output: [620, 7610, 630810]
  
  